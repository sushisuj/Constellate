# Base URL of services/orchestrator. Optional -- api.js falls back to
# http://127.0.0.1:8001 if this isn't set, which matches the port the
# orchestrator README tells you to run uvicorn on.
VITE_API_BASE=http://127.0.0.1:8001
